<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="exportkit.xd">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/wiggle_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".reset_password_activity"></activity>
        <activity android:name=".editprofile_activity" />
        <activity
            android:name=".home_activity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".buydog_detail_activity" />
        <activity android:name=".service_detail_activity" />
        <activity android:name=".buydogs_activity" />
        <activity android:name=".changepassword_activity" />
        <activity android:name=".profile_activity" />
        <activity android:name=".register_activity" />
        <activity android:name=".services_activity" />
    </application>

</manifest>